<%= simple_form_for(@improve) do |f| %>
  <div class="form-inputs">
    <%= f.input :flavor, as: :radio_buttons %>
    <%= f.input :texture, as: :radio_buttons %>
    <%= f.input :aroma, as: :radio_buttons %>
    <%= f.input :portion, as: :radio_buttons %>
    <%= f.input :visual, as: :radio_buttons %>
    <%= f.hidden_field :item_id, required: true, value: @item.id %>
    <%= f.hidden_field :score, required: true %>
    <div class="score"></div>
  </div>
  <br/>
  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>

<script>
  $(".score").raty({
    starOn: "<%= image_path('star-on.png')%>",
    starOff: "<%= image_path('star-off.png') %>",
    target: "#comment_score",
    targetKeep: true,
    targetType: "number"
  });
</script>